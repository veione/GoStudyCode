package main

type handle func(int)

type Router struct {
	handler map[string]handle
}

func (rt Router) add(name string, function handle) {
	rt.handler[name] = function
}

fun run(){

() {
err := recover()
stack := runtime
if err != nil {
fmt.Println("paic ")
}
}

func main() {

}
